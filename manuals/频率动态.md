# 频率动态使用手册

## 适用场景
- 事故后一次调频响应快估。
- 对比启用/不启用 AGC 的频率恢复趋势。

## 输入说明
- 额定频率 `f0`：通常 50 Hz。
- 功率缺额 `ΔP_OL0`：事故引起的有功失衡（pu）。
- `T_s`、`T_G`、`k_D`、`k_G`：分别对应惯性、调频时间常数、负荷阻尼与调频系数。

## 结果解读
- 重点关注最低频率、稳态偏差、恢复时间。
- 若 nadir 过低，可尝试增大 `k_G` 或缩短 `T_G`。
