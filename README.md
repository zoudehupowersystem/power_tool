# power_tool

电力系统工程近似计算 GUI 工具（Tkinter + Matplotlib）。

本项目用于工程场景下的**快速估算与参数折算**，覆盖频率动态、机电振荡、静稳极限、长线路自然功率、暂稳快估，以及线路/变压器参数校核与标幺值转换。

> ⚠️ 说明：本工具基于工程近似模型，不替代潮流、特征值分析、机电暂态时域仿真与正式校核。

---

## 1. 功能概览

`power_tool.py` 提供图形化界面，主要模块包括：

- 事故后频率动态快估（含一次调频二阶模型 + 无一次调频对照）
- 机电振荡频率估算
- 静态电压稳定极限估算
- 长线路自然功率与无功行为估算
- 暂态稳定：冲击法快估 + 等面积法（单机无穷大）
- 参数校核与标幺值转换：
  - 架空线路（π 型）
  - 两绕组变压器
  - 三绕组变压器

---

## 2. 运行环境

- Python 3.10+
- `numpy`
- `matplotlib`
- `tkinter`（多数 Python 发行版自带）

安装依赖：

```bash
pip install numpy matplotlib
```

---

## 3. 启动方式

在项目目录执行：

```bash
python power_tool.py
```

程序会打开 GUI 窗口。各页按输入项填写参数后点击计算按钮即可。

---

## 4. 三绕组变压器计算约定（重点）

本工具中的三绕组参数折算遵循以下约定：

1. **公共容量基准**采用高压侧额定容量 `SN_H`。
2. 三组短路损耗分解（T 型等值）前：
   - `Pk_HM / Pk_HL / Pk_ML` 均按对应绕组对的 `min(SN_i, SN_j)` 测试电流折算到 `SN_H`。

3. 三组短路电压 `Uk_HM / Uk_HL / Uk_ML`：
   - 直接按设备报参值参与分解，不对 `Uk_ML` 再做容量换算。
4. 分解后若某支路 `Uk%`（对应电抗）出现负值，程序会给出 **WARNING** 提示，提醒结合厂家试验数据复核（这在三绕组 T 型等值中可能出现，不必直接判错）。

---

## 5. 已做问题修复

本次对“三绕组变压器标幺值计算”相关逻辑进行了修正：

- 修复了公共容量基准使用方式：改为固定采用 `SN_H`。
- 修复了三组 `Pk` 折算电流容量选取：按各绕组对 `min(SN_i, SN_j)` 统一折算。
- 修复了 `Uk_ML` 被重复放大导致分解偏大的问题。
- 调整了三绕组分解后负支路的告警级别与提示语（由 ERROR 改为 WARNING）。

---

## 6. 注意事项

- 所有输入应使用同一组单位（kV、MVA、kW、%）。
- 变压器参数请尽量使用同一台设备同一试验报告的数据，避免“拼接参数”。
- 对告警项建议结合设备铭牌/试验报告复核，不建议盲目据此做运行边界决策。

---

## 7. 文件结构

- `power_tool.py`：主程序（GUI + 计算核心）
- `电力系统近似常数与近似公式.md`：近似常数与公式说明文档

